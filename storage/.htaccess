# Bloqueia todo o acesso web a esta pasta.
# Os ficheiros HTML das aplicações só são servidos através de app_raw.php,
# com sessão validada.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
