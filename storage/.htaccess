# Bloqueia todo o acesso web a esta pasta (backups, uploads).
# Os ficheiros só são servidos através de admin/backup.php, com sessão validada.

<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
